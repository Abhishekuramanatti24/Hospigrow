// utils/constants.js
// Centralized constants so magic strings for storage keys don't get
// scattered (and potentially mismatched) across the codebase.

export const STORAGE_KEYS = {
  TOKEN: 'sld_token',
  USER: 'sld_user',
};

export const ROUTES = {
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
};
